name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm run build
      - run: pnpm run lint
      - run: pnpm run format -- --check

      - name: Test CLI Generation
        run: |
          node dist/index.js create \
            --name "test-plugin" \
            --dir "test-output" \
            --build-tool "maven" \
            --language "kotlin" \
            --platform "purpurmc" \
            --version "1.21.1" \
            --ide "intellij" \
            --group-id "com.example" \
            --artifact-id "test-plugin" \
            --yes
        env:
          CI: true

      - name: Verify Output
        run: |
          ls -la test-output/test-plugin/
          grep -q "com.example" test-output/test-plugin/pom.xml && echo "✅ pom.xml replaced" || exit 1
        shell: bash
